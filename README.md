This projects runs a simulated call center application using cmd module for user and the queue for manage the upcoming calls.

Now this application runs as a client to communicate with the server. This application is responsible for interpret the command line and send message to the server, then print the result in stdout.